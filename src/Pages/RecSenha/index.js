import React, {Fragment, Component} from 'react';
import api from '../../Services/api';

export default class RecSenha extends Component{
    render(){
        return(
            <h1>email de resete de senha</h1>
        )
    }
}